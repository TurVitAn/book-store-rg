.hidden-xs.clearfix
  .dropdowns.pull-right
    .dropdown.width-240
      %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
        = t('.sort_by')
        %i.fa.fa-angle-down.dropdown-icon.pull-right
      %ul.dropdown-menu
        - t('books.partials.sorting.catalog_by').map do |key, value|
          %li
            = link_to value, books_path(category_id: params[:category_id], sort_by: key)
  %ul.list-inline.pt-10.mb-25.mr-240
    %li.mr-35
      %a.filter-link{href: books_path}
        = t('.all')
        %span#all_books.badge.general-badge
          = presenter.books_count
    - categories.each do |category|
      %li.mr-35
        %a.filter-link{href: books_path(category_id: category.id), data: { category_id: category.id }}
          = category.name
          %span.badge.general-badge
            = category.books.count
