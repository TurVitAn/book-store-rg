%main.container.general-main-wrap
  - if @order_items.blank?
    = render 'orders/partials/empty_order'
  - else
    %h1.general-title-margin
      = t('.title')
    .visible-xs
      = render partial: 'orders/partials/mobile/orders', locals: { order_items: @order_items }
    .hidden-xs
      = render partial: 'orders/partials/orders', locals: { order_items: @order_items }
    = render partial: 'orders/partials/order_summary', locals: { order: @order }
