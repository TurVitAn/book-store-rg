= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), method: :put, html: { id: 'edit-password' }) do |f|
  = f.input :current_password
  = f.input :password
  = f.input :password_confirmation
  .general-text-align
    = f.button :submit, t('.save')
