en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              invalid: must contain at least 1 uppercase, at least 1 lowercase, at least 1 number
